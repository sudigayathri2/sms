#apiVersion: v1
#kind: Service
#metadata:
#  name: employee-service      # for service
#  namespace: employee-app
#spec:
#  type: NodePort
#  selector:
#    app: employee
#  ports:
#    - protocol: TCP
#      port: 8200
#      targetPort: 8200
#      nodePort: 30090
apiVersion: v1
kind: Service
metadata:
  name: sms-service
spec:
  selector:
    app: sms-app
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
